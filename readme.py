"""
Day 1 Task :

1. installed vs code and python install manager

2. work with pip library manager  -> 

    version check->
    
    python -m pip --version 
    installed couple of libraries-> numpy and pandas

    i. pip install numpy
    ii.pip install pandas
    iii. pip uninstall numpy
    iv. pip show pandas
    v. pip list

    install all dependengitcies from requirements.txt file ->
    pip install -r requirements.txt

    pip freeeze 
    The Python pip freeze command is used to list packages that don't come pre-installed with Python. 

            Syntax: 

            pip freeze
    
    Python pip list --outdated command is used to list all the packages that are outdated. This command cross-checks the installed package information with the PIP repository.

            Syntax:

            pip list --outdated


3. virtual env -> to install all dependencies (eg. python libraries) at the same folder


    to create -> python -m venv venv
        python -m venv(name) venv(path)
    to activate-> .\venv\Scripts\Activate.ps1

4. git basics.


"""